# MATHCAD.md — перенос NF и SECT в Mathcad 15

Перенос проверенного Python-прототипа (`nf_sect.py`). Только две функции — `SECT` и `NF`. Логика, страховки и константы переносятся один-в-один, чтобы поведение совпадало с тестами.

## Карта Python → Mathcad

| Python | Mathcad 15 |
|---|---|
| `def f(...):` + `return` | `f(...) :=` + программная область |
| `while cond:` | `while cond` |
| `if c: A else: B` | `if c` / `otherwise` |
| `x ← x0` в программной области | `x ← x0` (присваивание в программе `←`) |
| `grad(x)` | `grad(x) ← ∇_x f(x)` и `hess(x) ← Jacob(grad(x), x)`, определены внутри `NF` |
| `np.linalg.solve(H,g)` | `H⁻¹ · g` |
| `H[0,0]`, `det(H)` | `H_{0,0}`, `|H|` |
| `np.linalg.norm(g)` | `|g|` |
| `d @ g` | `d · g` |
| лямбда | локальная функция через `←` внутри программной области |

## 1. Функции и производные

```text
f_quad(x,y)       := (x - 1)² + (y + 2)²
f_himm(x,y)       := (x² + y - 11)² + (x + y² - 7)²
f_rosen(x,y)      := (1 - x)² + 100·(y - x²)²
```

Функции цели (f_quad, f_himm, f_rosen) передаются в `NF` как аргумент. Градиент и гессиан не передаются — они вычисляются внутри `NF` через встроенные операторы `∇` и `Jacob`.

## 2. SECT — метод секущих

```text
SECT(g, a, b, eps) :=
    |  it ← 0
    |  while |g(b)| ≥ eps ∧ it < 500
    |      den ← g(b) - g(a)
    |      break if den = 0
    |      t ← b - g(b)·(b - a)/den
    |      a ← t if g(t)·g(a) > 0
    |      b ← t otherwise
    |      it ← it + 1
    |  b
```

## 3. NF — метод Ньютона-Рафсона

```text
NF(f, x0, eps1, eps2) :=
    |  grad(x) ← ∇ f(x)                            // градиент и гессиан
    |  hess(x) ← Jacob(grad(x), [x])                // определены прямо в NF
    |  x ← x0
    |  k ← 0
    |  while |grad(x)| > eps1 ∧ k < 500
    |      g ← grad(x)
    |      H ← hess(x)
    |      d ← -g                                    // по умолчанию градиентный спуск
    |      if H_{0,0} > 0 ∧ |H| > 1e-12
    |          dd ← -H⁻¹ · g
    |          if |dd| < 1e6 ∧ dd·g < 0           // спусковое и конечное
    |              d ← dd                             // ньютоновский шаг
    |      dphi(t) ← grad(x + t·d)·d                  // локальная функция — ПОСЛЕ выбора d
    |      a ← 0
    |      b ← 1
    |      while dphi(b) < 0 ∧ b < 100
    |          b ← 2·b
    |      t ← SECT(dphi, a, b, eps2)
    |      while f(x + t·d) ≥ f(x) ∧ t > eps2         // гарантия убывания
    |          t ← t/2
    |      x ← x + t·d
    |      k ← k + 1
    |  x
```

## 4. Вызов

```text
result ← NF(f_himm, (-1,-1), 1e-6, 1e-6)
// ожидаем ≈ (-3.78, -3.28)
```

## Замечания

- **`break`** в Mathcad 15 поддерживается — `SECT` и `NF` пишутся без флагов-костылей.
- **Лямбд нет** — вместо них внутри программной области `NF` объявляется локальная функция `dphi(t) ← grad(x + t·d)·d` (присваивание стрелкой `←`, не `:=`). `dphi` видит `x` и `d`, определённые выше в той же программе.
- **`dphi` объявляется ТОЛЬКО ПОСЛЕ выбора `d`** (после `if`-блока). В Mathcad локальная функция захватывает `x` и `d` в момент определения: если объявить её до `if`, она навсегда запомнит `d = -g`, и линейный поиск `t` будет вестись вдоль градиентного спуска, а шаг делаться вдоль ньютоновского направления — симптом «метод делает лишние шаги/двигается почти по прямой». В `nf_sect.py` `dphi` тоже создаётся после финализации `d`.
- **Производные** берутся символьно через `∇` и `Jacob` — это аналог `sp.diff` в Python.
- **Вырожденный Гессиан:** если обращение `-H⁻¹·g` не удалось (в Python это `LinAlgError`) — направление остаётся `d = -g` (градиентный спуск). В Mathcad то же самое: блок `if H_{0,0} > 0 ∧ det(H) > 1e-12` лишь предлагает ньютоновский шаг, при сбое или невыполнении условий `√(dd·dd) < 1e6` / `dd·g < 0` используется `d = -g`.
- **Константы** `eps1=1e-6`, `eps2=1e-6`, `M=500` (для Розенброка `main.py` задаёт `m=1000`), `det_tol=1e-12`, `d_max=1e6`, `b_max=100` — те же, что прошли тесты. Если в Mathcad появится «переполнение» — ищи потерянную страховку (порог детерминанта, кап направления или гарантию убывания) сравнением с `nf_sect.py`.
- **Число шагов зависит от `eps2`**. `while dphi(b) < 0 ∧ b < 100` разрешает шаг `t > 1` (до 2.68 на Розенброке): шаг перелетает изогнутую ложбину, и пара итераций Ньютона «ходит взад-вперёд» вокруг минимума — это ожидаемая **«ёлочка»**, метод при этом сходится сам. Эталон из репозитория (старт (2.25,2.75)): `eps2=1e-6` → 12 итераций монотонно, `eps2=1e-4`/`1e-3` → 13 итераций с «ёлочкой». Если в Mathcad вышло ~16 шагов — поставь `eps2=1e-8` (и `eps1=1e-6`), и число итераций совпадёт с эталоном; 12–16 шагов с переходом «ёлочкой» — нормальная числовая вариация одного и того же алгоритма.

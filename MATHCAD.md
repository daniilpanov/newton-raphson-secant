# MATHCAD.md — перенос NF и SECT в Mathcad 15

Перенос проверенного Python-прототипа (`algorithms/newton_raphson.py` — `nf`, `algorithms/secant.py` — `sect`). Логика, страховки и константы переносятся один-в-один, чтобы поведение совпадало с тестами.

## Карта Python → Mathcad

| Python | Mathcad 15 |
|---|---|
| `def f(...):` + `return` | `f(...) :=` + программная область |
| `while cond:` / `if c: A else: B` / `break` | `while cond` / `if c` / `otherwise` / `break` |
| `x ← x0` в программной области | `x ← x0` (присваивание в программе `←`) |
| `np.linalg.eigh(H)` | `eigenvals(H)`, собственный вектор через `eigenvec(H, λmin)` |
| `np.linalg.solve(H,g)` | `H⁻¹ · g` |
| `np.linalg.norm(g)` | `|g|` |
| `d @ g` | `d · g` |
| лямбда | локальная функция через `←` внутри программной области |
| `grad(x)`, `hess(x)` | `∇_x f(x)`, `Jacob(grad(x), x)`, определены внутри `NF` |

## 1. Функции и производные

```text
f_quad(x,y)       := (x - 1)² + (y + 2)²
f_himm(x,y)       := (x² + y - 11)² + (x + y² - 7)²
f_rosen(x,y)      := (1 - x)² + 100·(y - x²)²
f_dwell(x)        := x⁴ - 4·x² + (3/2)·x
```

Функции цели передаются в `NF` как аргумент. Градиент и гессиан не передаются — они вычисляются внутри `NF` через встроенные операторы `∇` и `Jacob`.

## 2. SECT — метод секущих

```text
SECT(g, a, b, eps) :=
    |  it ← 0
    |  while |g(b)| ≥ eps ∧ it < 500
    |      den ← g(b) - g(a)
    |      break if den = 0
    |      t ← b - g(b)·(b - a)/den
    |      a ← t if g(t)·g(a) > 0
    |      b ← t otherwise
    |      it ← it + 1
    |  a if |g(a)| < |g(b)|            // вернуть конец скобки с меньшим |g|
    |  b otherwise
```

## 3. NF — модифицированный метод Ньютона-Рафсона

```text
NF(f, x0, eps1, eps2) :=
    |  grad(x) ← ∇ f(x)                            // градиент и гессиан
    |  hess(x) ← Jacob(grad(x), [x])                // определены прямо в NF
    |  x ← x0 ; k ← 0
    |  while |g| > eps1 ∧ k < 500
    |      g ← grad(x) ; H ← hess(x)
    |      λ ← eigenvals(H) ; λmin ← min(λ)
    |      d ← -g                                    // по умолчанию градиентный спуск
    |      if λmin > 0                               // гессиан PD
    |          dd ← -H⁻¹·g
    |          if |dd| < 1e6 ∧ dd·g < 0           // спусковое и конечное
    |              d ← dd                             // ньютоновский шаг
    |      otherwise                                 // НЕ PD → отрицательная кривизна
    |          v ← собственный вектор для λmin
    |          d ← v if g·v ≤ 0 else -v              // ориентируем вниз
    |      dphi(t) ← grad(x + t·d)·d                  // локальная функция — ПОСЛЕ выбора d
    |      a ← 0 ; b ← 1 ; t* ← ∅
    |      while b < 100 ∧ t* = ∅
    |          while dphi(b) < 0 ∧ b < 100            // расширяем скобку до корня
    |              b ← 2·b
    |          if dphi(a)·dphi(b) ≤ 0                 // есть перемена знака
    |              t ← SECT(dphi, a, b, eps2)
    |              if dᵀ·hess(x + t·d)·d > 0          // МИНИМУМ вдоль d, не максимум
    |                  t* ← t
    |          a ← b ; b ← 2·b                        // иначе ищем следующий корень
    |      if t* = ∅                                  // корень-минимум не найден
    |          t ← 1
    |          while f(x + t·d) ≥ f(x) ∧ t > eps2
    |              t ← t/2
    |          t* ← t
    |      while f(x + t*·d) ≥ f(x) ∧ t* > eps2       // гарантия убывания
    |          t* ← t*/2
    |      x ← x + t*·d ; k ← k + 1
    |  x
```

## 4. Вызов

```text
result ← NF(f_himm, (0,2), 1e-6, 1e-6)
// цель: сойтись к (3, 2) без зигзагов через седло (исходно ~11 шагов)
```

## Замечания

- **НЕЛЬЗЯ применять классический шаг Ньютона при неположительно определённом гессиане.** Метод Ньютона ищет любой нуль `∇f = 0` — включая седло. Старый код проверял `H_{0,0} > 0 ∧ det(H) > 1e-12` и при неудаче сбрасывал направление на `d = -g`: из (0,2) градиентный спуск тянул к седлу (≈ (0.087, 2.884)), дальше «пологие зигзаги» по дну долины, ~10–11 шагов. Решение — собственные значения: при `λmin(H) ≤ 0` берётся собственный вектор `λmin`, ориентированный так, чтобы `g·v ≤ 0`. Первая же итерация выводит из седловой области в сторону минимума (3,2).
- **Поиск шага ищет корень, соответствующий минимуму вдоль направления.** Условие `φ'(t) = 0` выполняется и для максимума вдоль луча. После каждого корня из секущей проверяется кривизна `dᵀ·H(x+td)·d > 0`; корень-максимум пропускается, ищется следующий. На Химмельблау из (0,2) вдоль направления отрицательной кривизны есть ранний стационарный корень `t ≈ 0.0033` (маленький максимум, `f ≈ 90.0002`); без проверки кривизны он ловит алгоритм обратно к седлу. Если минимум-корень не найден вовсе — фолбэк на backtracking от `t=1` с гарантией убывания.
- **`SECT` возвращает лучший из концов скобки** (`a` или `b`, у кого `|g|` меньше), а не всегда `b`: на Химмельблау из (0,2) скобка упирается в `φ(1) ≈ 1.4e7`, секущие за 500 итераций не дожимают `b`, а возврат `b=1` давал перелёт ложбины. Лучший конец даёт плавный спуск.
- **Лямбд нет** — вместо них внутри `NF` объявляется локальная функция `dphi(t) ← grad(x + t·d)·d` (присваивание `←`, не `:=`). `dphi` видит `x` и `d`, определённые выше в программе.
- **`dphi` объявляется ТОЛЬКО ПОСЛЕ выбора `d`**: локальная функция захватывает `x` и `d` в момент определения; если объявить её до `if`, она навсегда запомнит `d = -g`. В `newton_raphson.py` `dphi` тоже создаётся после финализации `d`.
- **Вырожденный гессиан:** если `-H⁻¹·g` не посчитался (`LinAlgError`) или проверки `|dd| < 1e6` / `dd·g < 0` не прошли — используется `d = -g`.
- **Константы** `eps1=1e-6`, `eps2=1e-6`, `M=500` (для Розенброка `main.py` задаёт `m=1000`), `d_max=1e6`, `b_max=100` — те же, что прошли тесты. При «переполнении» — сравнивать с `newton_raphson.py`.
- **Число шагов зависит от `eps2`.** `while dphi(b) < 0 ∧ b < 100` разрешает шаг `t > 1`: перелёт ложбины и «ёлочка» вокруг минимума — нормальная вариация алгоритма; при расхождении числа шагов ставить `eps2=1e-8` при `eps1=1e-6`.
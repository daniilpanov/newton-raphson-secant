# MATHCAD-SIMPLE.md — перенос «чистой» версии NF и SECT в Mathcad 15

Перенос проверенного Python-прототипа `nf_sect_simple.py` — **канонический («книжный») метод Ньютона-Рафсона** и **метод секущих** по Пантелееву А.В. «Методы оптимизации в примерах и задачах» (2005). В отличие от `nf_sect.py`, здесь **нет** инженерных модификаций: нет проверки положительной определённости Гессиана, нет капа длины направления, нет отката на градиентный спуск, нет backtracking. Единственная добавленная деталь — внутри `NF` для поиска шага используется `SECT` (по формуле `x_{k+1} = x_k + t·d`, где `t` ищется из `φ'(t)=0`).

## Карта Python → Mathcad

| Python | Mathcad 15 |
|---|---|
| `def f(...):` + `return` | `f(...) :=` + программная область |
| `while cond:` | `while cond` |
| `if c: A else: B` | `if c` / `else` |
| `break` | `break` (поддерживается) |
| `x ← x0` в программной области | `x ← x0` (присваивание в программе `←`) |
| `grad(x)` | `grad(x) ← ∇_x f(x)` и `hess(x) ← Jacob(grad(x), x)`, определены внутри `NF` |
| `np.linalg.solve(H,g)` | `H⁻¹ · g` |
| `np.linalg.norm(g)` | `√(g · g)` |
| `d @ g` | `d · g` |
| лямбда | локальная функция через `←` внутри программной области |

## 1. Функции и производные

```text
f_quad(x,y)       := (x - 1)² + (y + 2)²
f_himm(x,y)       := (x² + y - 11)² + (x + y² - 7)²
f_rosen(x,y)      := (1 - x)² + 100·(y - x²)²
```

Функция цели передаётся в `NF` как аргумент. Градиент и гессиан — по канону — вычисляются внутри `NF` через встроенные операторы `∇` и `Jacob`.

## 2. SECT — метод секущих

```text
SECT(phi_prime, L, R, eps) :=
    |  z ← L
    |  while |phi_prime(z)| ≥ eps
    |      den ← phi_prime(R) - phi_prime(L)
    |      if den = 0
    |          break
    |      newz ← L - phi_prime(L)·(R - L)/den
    |      if phi_prime(newz)·phi_prime(L) > 0
    |          L ← newz
    |      else
    |          R ← newz
    |      z ← newz
    |  z
```

Замечание: цикл ведётся по оценке корня `z` (а не по `L`), чтобы не зависнуть на точном корне, где `φ'(z)=0` делает проверку `φ'(newz)·φ'(L) > 0` вечно ложной.

## 3. NF — метод Ньютона-Рафсона с шагом через SECT

```text
NF(f, x0, eps, eps2) :=
    |  grad(x) ← ∇ f(x)                            // градиент и гессиан
    |  hess(x) ← Jacob(grad(x), [x])                // определены прямо в NF
    |  x ← x0
    |  k ← 0
    |  while √(grad(x)·grad(x)) > eps ∧ k < 500
    |      g ← grad(x)
    |      H ← hess(x)
    |      d ← -H⁻¹ · g                             // направление Ньютона, без проверок
    |      dphi(t) ← grad(x + t·d)·d                 // локальная функция (стрелка ←)
    |      t ← SECT(dphi, 0, 1, eps2)               // поиск шага методом секущих
    |      x ← x + t·d
    |      k ← k + 1
    |  x
```

## 4. Вызов

```text
result ← NF(f_himm, (-1,-1), 1e-6, 1e-6)
// ожидаем ≈ (-3.78, -3.28)
```

## Ожидаемое поведение (важно для отчёта)

- **Функция №1 (квадратичная):** сходится за 1 итерацию — чистый Ньютон точен на квадратичной функции.
- **Химмельблау:** метод частично расходится/осциллирует или медленно ползёт (зависит от стартовой точки) — классическое локальное поведение канонического Ньютона без страховок.
- **Розенброк:** медленная сходимость (до `M=1000` итераций) с ошибкой ~1e-4 — из-за «овражной» структуры и почти точного шага SECT.

> «В данной реализации используется канонический метод Ньютона-Рафсона без модификаций. Сходимость гарантируется только локально для сильно выпуклых функций (функция №1). На невыпуклых и многоэкстремальных функциях (Химмельблау, Розенброка) метод демонстрирует расхождение, осцилляции или медленную сходимость, что является классическим поведением алгоритма в его чистой формулировке.»

## Замечания

- **`break`** в Mathcad 15 поддерживается — функции пишутся без флагов-костылей.
- **Лямбд нет** — вместо них внутри программной области `NF` объявляется локальная функция `dphi(t) ← grad(x + t·d)·d` (присваивание стрелкой `←`, не `:=`). `dphi` видит `x` и `d`, определённые выше в той же программе.
- **Производные** берутся символьно через `∇` и `Jacob` — это аналог `sp.diff` в Python.
- **Нет** проверки `det(H)` перед обращением: если `H` вырождена — обращение `H⁻¹` выдаст ошибку, программа остановится. Это нормально для чистой версии (см. `nf_sect_simple.py:66-70`, где аналогично просто прерывается).
- Константы `eps=1e-6`, `eps2=1e-6` (критерий для `SECT` при поиске шага), `M=500` (для Розенброка `M=1000`) — те же, что в прототипе.
